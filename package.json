{
  "name": "yu256.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:pug": "pug --out ./ --watch ./index.pug ./blog.pug --pretty",
    "build:sass": "sass --no-source-map ./css/",
    "watch:sass": "sass --no-source-map --watch ./css/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pug-cli": "1.0.0-alpha6",
    "sass": "^1.59.3"
  }
}
